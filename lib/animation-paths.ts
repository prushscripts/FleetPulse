/**
 * Canonical paths for animation assets. All animation media lives under public/animations (lowercase).
 * Use these constants everywhere to avoid casing/404 issues and fallback duplication.
 */
export const ANIMATIONS_BASE = '/animations'

export const INTRO_VIDEO = `${ANIMATIONS_BASE}/officialfpanimation.mp4`
export const LOGO_LOOP_VIDEO = `${ANIMATIONS_BASE}/possiblelogoloop.mp4`
