/**
 * OG image is now public/images/bottom1.png (your FleetPulse logo).
 * Sharp was removed to fix Vercel "Maximum call stack size exceeded" during build traces.
 * To save the live OG image from production: node scripts/fetch-og-image.js
 */
console.log('OG image: use public/images/bottom1.png. To fetch from production run: node scripts/fetch-og-image.js')
