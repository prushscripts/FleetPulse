banner1.png = primary (link preview, navbar, login, signup). banner2.png = optional variant.
Transparency: Keep the gradient background (don't make them transparent). They're designed to look good as cards; transparent would make the text hard to read on some backgrounds.
