Add your main FleetPulse logo here so it shows on signup, login, navbar, and link preview:
- bottom1.png = main logo (the brighter purple "FleetPulse" card). Required.
- bottom2.png = optional darker variant.
If your file is named banner1.png, rename it to bottom1.png or the app won't find it.

Transparency: Keep the gradient background on the logos (don't make them transparent). They're designed to look good as cards; transparent would make the text hard to read on some backgrounds.
