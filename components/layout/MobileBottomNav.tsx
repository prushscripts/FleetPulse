'use client'

/**
 * Legacy placeholder.
 *
 * The active app uses `components/app/BottomTabBar.tsx`.
 * Some archived backup code still imports `MobileBottomNav`; keeping a stub
 * prevents build-time type errors without rendering duplicate nav.
 */
export default function MobileBottomNav() {
  return null
}

